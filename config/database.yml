development:
  adapter: postgresql
  encoding: unicode
  database: journalclub_development
  pool: 8
  username:
  password:
  host: localhost
  port: 5432

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # The server defaults to notice.
  #min_messages: warning

test:
  adapter: postgresql
  encoding: unicode
  database: journalclub_test
  pool: 5
  username:
  password:
  host: localhost
  port: 5432
  min_messages: warning
